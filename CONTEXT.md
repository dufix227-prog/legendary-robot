# КОНТЕКСТ ДЛЯ СЛЕДУЮЩЕЙ СЕССИИ (handoff, обновлён 25.09 — ФУЛЛ-ПРОХОД ВЫПОЛНЕН)

Прочитай это первым делом.

## Статус: план 10 «ФУЛЛ-ПРОХОД» выполнен — все 12 блоков, 12 коммитов

Все гейты зелёные, `tests/smoke.py` 21/21. Канон решений — `docs/plans/09-owner-decisions-2026-09-24.md`.

**Что сделано (коммиты 1–12 в репо `logovo-clone/`):**
1. Блок 0: venv, config с dotenv и всеми настройками 09/10, порт 9542, standalone-сервер.
2. Блок 1: схема 35 таблиц (+миграции колонок), elo ранги от 1000 (Окурок…Легенда).
3. Блок 2: валидатор initData + окно auth_date 24ч, заморозка 403 с причиной.
4. Блок 3: бот-каркас: /start+game_nickname, меню 8 кнопок, 🐞 Связь работает.
5. Блок 4: турнирное ядро: /season /cup /club /calendar /tour /pairs /editpair /judge;
   round-robin, кубковые сетки (серии до 2 побед, пенальти), таблица.
6. Блок 5: OCR-каскад (OpenRouter×7 → Groq → Gemini → NIM → OCR.space → tesseract),
   промпт по плану 06; репорт скринами → финализация СРАЗУ; спор 24ч → судья /resolve;
   ручной ввод /manual; штраф неигранным; Elo клубы+игроки; гейт 11 эталонов «samples/fc27-screens».
7. Блок 6: мини-апп SPA (6 вкладок), API ядра, кэфы Пуассон от Elo + маржа 5%,
   тест-сид `tests/seed_data.py` (10 клубов с лого, тур сыгран, линия с кэфами).
8. Блок 7: ставки: купон single/express 5 ног, ODDS_CHANGED, idempotency_key,
   авто-обрез выплаты, лимиты оригинала, запреты владелец/судья, авто-расчёт при
   финализации, XP/уровни, бонус-серия 50→+10→кап150, промокоды, рынки противостояния кубков.
9. Блок 8: трансферы: маркетплейс фикс/аукцион, свободные агенты рейтинг²×K,
   обмены (деньги/игрок+деньги/игрок↔игрок), порог 2М → судья, комиссия 5%, экран «Рынок».
10. Блок 9: долги + напоминания 3×/день (reminder_log по дате); парсер Challenge Place
    — фикстура реального турнира снята браузером (`tests/cp_fixture.json`: 31 competitor,
    3 матча со счётами, серии), /scan файл|url, импорт помечен source, расхождения → алерт root.
11. Блок 10: админка: API (dashboard, bets+void, пауза глоб/матч, игроки+adjust/ban,
    настройки, exposure, audit, перерасчёт), панель в аппе для is_admin, root-команды
    /promo /admin.
12. Блок 11: `tests/smoke.py` полный (21 проверка), `tests/wipe.py`.

**Запуск:** `cd logovo-clone && venv/bin/python bot/main.py` (бот+апп одним процессом)
или только апп: `venv/bin/python -m miniapp.server` → http://127.0.0.1:9542/app
Тест-данные: `venv/bin/python tests/seed_data.py` (снос: `tests/wipe.py`).
Скриншоты ядра сняты 25.09: линия, купон, таблицы+результаты, кабинет, клуб,
карточка матча, рынок трансферов, админ-панель, уведомления.

## Ревью-проход 25.09 (после ФУЛЛ-ПРОХОДА)
- **Бот не стартовал**: PTB запрещает кириллицу в командах → все команды переведены на латиницу
  (`/season`, `/resolve`, `/manual`…), меню «/» выставляется через setMyCommands (root — полное).
  Новые: `/nick`, `/paid <id>`, `/myid`.
- **Мини-апп в Telegram не авторизовался**: не был подключён `telegram-web-app.js`. Кнопка
  «Открыть мини-апп» и menu button появляются, если `WEBAPP_PUBLIC_URL` = https.
- Деньги: пересчёт купонов при споре/правке счёта (`bets_engine.unsettle_for_match`), серии кубка
  (рынки, точный счёт, tie-ноги в экспрессе), финал сезона без двойных призовых, трансфер-дыры,
  AH +1.5, проигранная нога рассчитывает экспресс сразу.
- Уведомления о расчёте купона — формат оригинала (`bot/bet_notify.py`), дублируются в ЛС
  (`job_push_bet_results`, выключить: bot_settings `notify_bets_dm=0`).
- Дизайн мини-аппа: шрифты Manrope/Unbounded локально (`static/fonts`, OFL), SVG-навигация, фиксы
  бейджа/таблицы/истории ставок. Названия клубов с заглавной (миграция в `db._migrate`).
- OCR: `gemini-2.0-flash` выключен Google 01.06.2026 → `GEMINI_MODEL` (деф. gemini-3.5-flash),
  `OPENROUTER_MODELS` из env, локальная модель через Ollama (`OLLAMA_URL`, `qwen2.5vl:3b`).
- OCR только бесплатный (25.09): Groq убран (vision-модель снята с продакшена, тарифы платные);
  все 7 старых OpenRouter-моделей были удалены с площадки → новые `:free` (Gemma 4, Qwen 3.8,
  `openrouter/free`) + фильтр по живому каталогу (`ocr._openrouter_alive`), платные слаги не пускаются.
  Порядок: Gemini → OpenRouter free → NIM → Ollama → OCR.space → tesseract.
- Тесты: `gate11_fixes.py`, `gate12_bot.py` (+ все прежние) — зелёные.

## Карточки игроков (25.09)
`bot/cards.py`, `bot/card_ocr.py`, `bot/renderz.py`, `bot/schema_cards.py`, `miniapp/routes_cards.py`,
`static/features/cards.js`; контракт — `docs/API-cards.md`. Владелец добавляет карточку в «Клуб»
(фото → OCR-автозаполнение, ссылка RenderZ → сверка имя/OVR/позиция, или вручную) → `pending` →
судья/админ одобряет (или авто при полном совпадении с RenderZ). Непроверенные нельзя продать/обменять.
RenderZ: только одна страница по ссылке от игрока (robots.txt запрещает /api/*, ToS — раздачу их базы),
их статы/картинки не храним. Фото — `data/media/cards/` (вне git, EXIF срезается). Рынок: поиск, позиция
(+доп.), OVR/цена, тип, сборная/лига, минимумы статов, сортировка. Статика отдаётся с `Cache-Control:
no-cache`. Тест: `tests/gate20_cards.py`.

## Лиги с названиями дивизионов и участники (25.09)
`bot/league_admin.py` + `miniapp/routes_leagues.py` + `static/features/leagues.js`.
Админка → «🏆 Лиги и участники»: сезон с дивизионами по названиям (Ла Лига, Серия А, Лига 1, Лига 2,
Серия С…), порядок ↑↓ = иерархия, `tournaments.promote_count` (0 — независимые лиги, без ↑↓),
клубы из каталога или свои (без лого), владелец по @нику/ID, календарь по всем дивизионам.
«👥 Ники и клубы»: ник FC27 (уникальный с нормализацией, как в report_match), клуб, судья (только root),
предрегистрация игрока по Telegram ID до /start. Бот: `/season … divisions=Ла Лига, Серия А promote=3`,
`/setnick <@user|ID> <ник>`. Исправлено: календарь второго дивизиона стирал туры первого.
Тест: `tests/gate19_leagues.py`.

## Доделки «делай всё» (25.09)
Каркас: схема фич — `bot/schema_*.py` (SCHEMA + migrate, автоподключение в `db.init_db`), API фич —
`miniapp/routes_*.py` с `setup(app)` (автоподключение в `server.build_app`), фронт — `static/lib.js`
(утилиты + `hooks.views/profileCards/clubCards/adminCards`, `openSheet`) и `static/features/*.js|css`.
- Кубки: авто-стадии до финала (`league.sync_cup`), байи, перестройка пары при смене победителя,
  `cup_results` + одна выплата приза; `/cupnext`, `/bracket`; вкладка «Кубки» в Таблицах.
- Баланс при пересчёте: по умолчанию списываем до нуля (`resettle_allow_negative=0`), недостача в аудит.
- Трансферы: аукционы (шаг, блокировка суммы, антиснайпинг, выкуп, судья выше порога),
  job `job_transfers` раз в минуту закрывает аукционы и шлёт ЛС; обмены из аппа; очередь судьи.
- Прогресс: 30 достижений (ленивый расчёт, награда кнопкой), Зал славы (баланс/выиграно/чемпионы,
  таблица `hall_of_fame`), fair-play учёт тренировок с лимитом и нарушениями в админке.
- Админка: поиск игроков, выдать/снять админа (root), редактор настроек с валидацией
  (GET настроек раньше падал 500).
- Challenge Place по ссылке: `bot/cp_fetch.py` (plain → Playwright). Реальный турнир закрыт Cloudflare
  Turnstile → бот просит сохранить страницу и дать файл. Деплой: `deploy/` + `docs/DEPLOY.md`.
- Тесты: `gate15_cup`, `gate16_transfers`, `gate17_progress`, `gate18_infra`.

## Свои OCR-провайдеры (25.09)
Мини-апп → Кабинет → 👮 → «🧠 OCR-провайдеры» (только root): любой OpenAI-совместимый API
(base URL + модель + ключ), «первым» или «запасным», вкл/выкл, «Проверить» — прогон эталонного
скрина (ожидается 2:3, temiyy/Rusli). Таблица `ocr_providers`, ключ только в БД, наружу маской.
Проверено вживую: AMD `developer.amd.com.cn/radeon/api/v1` + `DeepSeek-V4.1-Flash` читает скрин верно.
Тест: `tests/gate14_ocr_providers.py`.

## Английские имена (25.09)
Папки/файлы/команды — латиницей: `bot/`, `miniapp/`, `tests/` (`gate*.py`, `seed_data.py`, `wipe.py`,
`references.py`), `samples/fc27-screens/`, `docs/plans/`, `docs/sessions/`. Тексты игрокам — русские.
Параметры команд: `divisions= rounds= days= format= clubs= division= pens= goals=` (русские тоже работают).
Root = `ADMIN_IDS` в `bot/.env` (6163072393), сразу админ мини-аппа; свой ID — `/myid`.

## Приём скринов (25.09, `bot/report_match.py`)
На скрине FC27 нет наших клубов/дивизионов — только ники игроков и их игровые клубы.
Цепочка: ник со скрина → `players.game_nickname` (нечётко: опечатки OCR, кириллица/латиница)
→ клуб → несыгранный матч (открытый или закрытый тур, кубок) → турнир/дивизион/тур.
Кто слева на скрине — тоже по никам (слева в FC27 ≠ хозяин в календаре → счёт разворачивается).
Скрины копятся пачкой 4 с (до 3), один прогон OCR. Ники не узнаны → кнопки «какой матч» /
«кто слева». Чужой матч — отказ. Тест: `tests/gate13_screens.py`.

## 4 паузы из плана 10 — статус
1. **Живой /start боту** — НЕ сделан (владелец спал). Бот-каркас готов, getMe ок.
   При пробуждении: `/start` → сохранить ник FC27.
2. **Свежие скрины FC27** — не нужны пока: 11 эталонов хватило для гейта.
   Живой OCR потребует ключи в .env (OPENROUTER_API_KEY или любой из каскада).
3. **Выбор палитры** — дефолт ИЗУМРУД применён (выбор владельца 25.09); пресеты coal/gold вшиты
   (`localStorage.theme`), показать владельцу скриншотами.
4. **VPS** — ждёт явной команды «деплой» (план 09: сначала локально, потом VPS руками).

## Ставки, клубы, рейтинги (25.09, гейты 21–22)
- **Линия 35 рынков** (`bot/markets.py`): +двойной шанс, ТБ/ТМ 1.5 и 3.5, «забьёт» (ИТБ 0.5),
  точный счёт (15 вариантов). Вероятность и расчёт ноги — одна таблица `_WINS`, матрица Пуассона
  нормирована; кэф не ниже 1.01. Старые матчи получают новые рынки при `refresh_tour`/`generate_markets`.
- **Кэшаут** (`bot/bet_tools.py`): выплата × P(оставшиеся ноги) × (1 − `cashout_margin_pct`, деф. 8%),
  только пока тур открыт и матч pending (то же окно, что приём ставок — прематч, как в решении 09),
  без серий (tie_*). Статус купона `cashout`, сумма в `bets.cashout_amount`. Сменилась квота →
  `CASHOUT_CHANGED` с новой суммой. Выключатель `cashout_enabled`.
  ⚠️ Решение 09 говорило «cashout не делаем» — это было про лайв; владелец попросил кэшаут 25.09.
- **Черновики купонов** (`saved_coupons` + `name`, лимит `saved_coupons_max`=10): при загрузке
  закрытые матчи отбрасываются, кэфы свежие.
- **Статистика каппера**: ROI (void не входит), проходимость won/(won+lost), средний кэф, серии,
  разбивка по группам рынков. **Рейтинги капперов** по сезону/дивизиону: купон в зачёте, если все
  его ноги на матчах сезона/дивизиона; сортировка по прибыли.
- **Аналитика матча** (`bot/match_insights.py`): H2H, форма, статы клубов в турнире, подсказки;
  «Как получен кэф» — Elo → λ → Пуассон → маржа. **Value**: вторая модель по фактическим голам
  (вес до 70% к 10 матчам) выше 1/кэф на `value_edge_pp` (5 п.п.) при `value_min_games` (3).
- **Клубы** (`bot/club_economy.py`, `schema_clubs.py`): цвета, эмодзи-эмблема (вместо лого в
  линии/таблицах), девиз, стадион. Доход со стадиона за домашний матч (`stadium_income_base` × уровень ×
  W 1.25/D 1/L 0.8), апгрейд 5 уровней за бюджет; спонсор на сезон (3 варианта, бонус за подпись +
  матч/победа/ничья, `sponsor_scale_pct`). Всё в `club_ledger`; правка счёта откатывает и начисляет
  заново (`results.finalize_match`). Выключатель `club_income_enabled`.
- **Время матча**: участники/судья/админ ставят `matches.scheduled_at` (UTC) в карточке матча,
  второй стороне уведомление. На окно ставок не влияет.
- Фронт: `features/betting.js`, `features/clubs.js`; из фич перерисовка через `lib.actions`.

## Postgres, тестовый сезон, справочник (25.09, гейты 23–24)
- **Postgres впервые проверен вживую** (16.x): раньше `init_db` падал на схеме. Исправлено в
  `db_backend`: `datetime('now'[, '±N unit'])` → текст UTC того же формата, что SQLite; скалярные
  `MAX/MIN(a,b)` → `GREATEST/LEAST`; литеральный `%` экранируется; `SUM()` без `Decimal`;
  строки поддерживают `row[0]`. Все гейты: `PG_URL=postgresql://user@host:port tests/run_all.sh`
  (каждый набор на своей базе `t_<набор>`), SQLite — просто `tests/run_all.sh`.
- **Баг:** генерация календаря открывала тур 1 без кэфов (линия пустая до ручного «обновить кэфы»).
  Теперь `generate_league_calendar` сразу строит рынки тура 1.
- **gate23_season** — полный сезон на реальном коде (2×6 клубов, 10 туров, 16 капперов, кэшауты,
  правки счёта, спонсоры, финал с призовыми). Инварианты денег + отчёт по экономике
  (`SEASON_SEED=N` — другой сезон). Выводы: доход линии сильно плавает на малых суммах;
  «value-охотники» стабильно в плюсе (+3…+79% ROI) — подсветка знает фактические голы, а линия
  считается только по Elo. Спонсоры перебалансированы (рисковый ≈9 млн, надёжный 8 млн за сезон).
- **Справочник реальных игроков** (`bot/players_directory.py`): открытый датасет
  dcaribou/transfermarkt-datasets (CC0), сайт Transfermarkt не парсим, фото не берём. ~28 тыс.
  активных игроков, снимок на дату (кнопка в админке root или `venv/bin/python bot/players_directory.py`).
  Автокомплит имени в форме карточки (в т.ч. по-русски) подставляет позицию/сборную/клуб/лигу/ногу/рост.
- **Challenge Place по ссылке**: Playwright ставится и запускается, но с IP дата-центра Cloudflare
  отдаёт «Один момент…». С домашнего IP не проверено; запасной путь — `/scan <файл.html>`.
  Обход защиты не делаем.

## Challenge Place
Фикстура реального дашборда сохранена; парсер работает на файле/HTML. Живой URL-скан
требует headless-браузер: `pip install playwright && playwright install chromium` в venv
(на SteamOS не ставил — тяжёлая зависимость), либо сохранять страницу и давать файл.

## Грабли (проверено на этом проходе)
- `python miniapp/server.py` — только через `-m miniapp.server` (пакет с относительными импортами).
- Схема живёт с миграциями `_migrate()` в db.py — CREATE IF NOT EXISTS колонки не добавляет.
- `LIMIT 1` без `ORDER BY id` на matches может вернуть матч не того тура.
- Курсор db_backend не итерировать напрямую; каждая транзакция — commit перед close.
- Секреты только в .env (токен бота в bot/.env); в git не коммитить.
- acc2 (Telethon, ~/Desktop/боты/prompt-bot/) — для живого /start; после дел погасить сессию.
